app:
  interface: repl
  permissions:
    no_rules_rules: true

agent:
  provider: anthropic
  model: claude-sonnet-4-6
  systemPrompt: You are an autonomous ML research agent. You iteratively modify training code, run experiments, evaluate results, and keep or discard changes without human intervention.
  maxIterations: 500
  toolTimeout: 900000
  tools:
    builtin: true
    overrides:
      Agent:
        enabled: false
      AskUserQuestion:
        enabled: false
      WebFetch:
        enabled: false
  compaction:
    enabled: true
    threshold: 0.8
