agent:
  provider: anthropic
  model: claude-sonnet-4-6
  skillDirs:
    - ./skills
  middleware:
    afterModelResponse:
      - ./middleware/token-budget.ts
  maxIterations: 10
  compaction:
    enabled: true
    threshold: 0.8
