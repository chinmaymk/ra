import{_ as e,o as a,c as n,ai as i}from"./chunks/framework.C0QefDMS.js";const m=JSON.parse('{"title":"Sessions","description":"","frontmatter":{},"headers":[],"relativePath":"concepts/sessions.md","filePath":"concepts/sessions.md","rawMarkdown":"# Sessions\\n\\nSessions are how ra persists conversations. Every time you interact with ra — whether through the CLI, REPL, HTTP API, or cron — it creates a session that records the full message history.\\n\\n## What\'s stored\\n\\nEach session lives in its own directory:\\n\\n```\\n~/.ra/<project>/sessions/<id>/\\n  meta.json         # metadata: id, created, provider, model, interface\\n  messages.jsonl    # append-only message log\\n  logs.jsonl        # structured logs (when enabled)\\n  traces.jsonl      # trace spans (when enabled)\\n```\\n\\nMessages are stored as JSONL (one JSON object per line, appended as they happen). This means sessions survive crashes — you never lose conversation history.\\n\\n## Resuming sessions\\n\\nIn the REPL, resume your last session:\\n\\n```\\n› /resume\\n```\\n\\nOr resume a specific session by ID. Your full conversation history is restored, and you pick up where you left off.\\n\\n## Session scoping\\n\\nSessions are scoped per project. Each project (identified by its data directory) has its own set of sessions, logs, and traces. This keeps work on different projects cleanly separated.\\n\\n## Auto-pruning\\n\\nSessions don\'t pile up forever. Configure limits to keep things tidy:\\n\\n```yaml\\napp:\\n  storage:\\n    maxSessions: 100    # keep the most recent 100\\n    ttlDays: 30         # delete sessions older than 30 days\\n```\\n\\n## Memory\\n\\nSeparate from session history, ra has a persistent memory system backed by SQLite with full-text search. Memory lives across sessions — the agent can save facts, search for them later, and forget them when they\'re no longer relevant.\\n\\n```yaml\\nagent:\\n  memory:\\n    enabled: true\\n    maxMemories: 1000\\n    ttlDays: 90\\n```\\n\\nWhen enabled, the agent gets three tools: `memory_save`, `memory_search`, and `memory_forget`. Middleware can also inject relevant memories at the start of each loop automatically.\\n\\nMemory is useful for things like user preferences, project conventions, and facts the agent learns over time that shouldn\'t be tied to a single conversation.\\n\\nSee [Sessions reference](/core/sessions) and [Memory](/tools/#memory) for full details.\\n"}'),t={name:"concepts/sessions.md"};function o(r,s,l,p,h,d){return a(),n("div",null,[...s[0]||(s[0]=[i(`<h1 id="sessions" tabindex="-1">Sessions <a class="header-anchor" href="#sessions" aria-label="Permalink to &quot;Sessions&quot;">​</a></h1><p>Sessions are how ra persists conversations. Every time you interact with ra — whether through the CLI, REPL, HTTP API, or cron — it creates a session that records the full message history.</p><h2 id="what-s-stored" tabindex="-1">What&#39;s stored <a class="header-anchor" href="#what-s-stored" aria-label="Permalink to &quot;What&#39;s stored&quot;">​</a></h2><p>Each session lives in its own directory:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>~/.ra/&lt;project&gt;/sessions/&lt;id&gt;/</span></span>
<span class="line"><span>  meta.json         # metadata: id, created, provider, model, interface</span></span>
<span class="line"><span>  messages.jsonl    # append-only message log</span></span>
<span class="line"><span>  logs.jsonl        # structured logs (when enabled)</span></span>
<span class="line"><span>  traces.jsonl      # trace spans (when enabled)</span></span></code></pre></div><p>Messages are stored as JSONL (one JSON object per line, appended as they happen). This means sessions survive crashes — you never lose conversation history.</p><h2 id="resuming-sessions" tabindex="-1">Resuming sessions <a class="header-anchor" href="#resuming-sessions" aria-label="Permalink to &quot;Resuming sessions&quot;">​</a></h2><p>In the REPL, resume your last session:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>› /resume</span></span></code></pre></div><p>Or resume a specific session by ID. Your full conversation history is restored, and you pick up where you left off.</p><h2 id="session-scoping" tabindex="-1">Session scoping <a class="header-anchor" href="#session-scoping" aria-label="Permalink to &quot;Session scoping&quot;">​</a></h2><p>Sessions are scoped per project. Each project (identified by its data directory) has its own set of sessions, logs, and traces. This keeps work on different projects cleanly separated.</p><h2 id="auto-pruning" tabindex="-1">Auto-pruning <a class="header-anchor" href="#auto-pruning" aria-label="Permalink to &quot;Auto-pruning&quot;">​</a></h2><p>Sessions don&#39;t pile up forever. Configure limits to keep things tidy:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">app</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  storage</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    maxSessions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">100</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # keep the most recent 100</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    ttlDays</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">30</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">         # delete sessions older than 30 days</span></span></code></pre></div><h2 id="memory" tabindex="-1">Memory <a class="header-anchor" href="#memory" aria-label="Permalink to &quot;Memory&quot;">​</a></h2><p>Separate from session history, ra has a persistent memory system backed by SQLite with full-text search. Memory lives across sessions — the agent can save facts, search for them later, and forget them when they&#39;re no longer relevant.</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">agent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  memory</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    enabled</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    maxMemories</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1000</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    ttlDays</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">90</span></span></code></pre></div><p>When enabled, the agent gets three tools: <code>memory_save</code>, <code>memory_search</code>, and <code>memory_forget</code>. Middleware can also inject relevant memories at the start of each loop automatically.</p><p>Memory is useful for things like user preferences, project conventions, and facts the agent learns over time that shouldn&#39;t be tied to a single conversation.</p><p>See <a href="/ra/v/0.0.6/core/sessions.html">Sessions reference</a> and <a href="/ra/v/0.0.6/tools/#memory">Memory</a> for full details.</p>`,21)])])}const y=e(t,[["render",o]]);export{m as __pageData,y as default};
