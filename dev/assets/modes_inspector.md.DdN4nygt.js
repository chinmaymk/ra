import{_ as s,o as a,c as i,ai as n}from"./chunks/framework.Cz-NuZmh.js";const t="/ra/dev/inspector-overview.png",o="/ra/dev/inspector-timeline.png",k=JSON.parse('{"title":"Inspector","description":"","frontmatter":{},"headers":[],"relativePath":"modes/inspector.md","filePath":"modes/inspector.md","rawMarkdown":"# Inspector\\n\\nA standalone web dashboard for debugging agent sessions. Serves a single-page app that lets you browse sessions, inspect messages, view token usage, and trace every tool call the agent made.\\n\\n```bash\\nra --inspector                          # launch on default port 3002\\nra --inspector --inspector-port 8080    # custom port\\n```\\n\\n> The inspector is a standalone interface — it reads saved session data from disk. Run your agent normally (CLI, REPL, HTTP, etc.), then launch the inspector separately to review sessions.\\n\\nOpen `http://localhost:3002` in your browser.\\n\\n## Views\\n\\n![Inspector Overview](/inspector-overview.png)\\n\\n### Session views\\n\\nSelect a session from the sidebar to see its data across five tabs:\\n\\n| Tab | What it shows |\\n|-----|---------------|\\n| **Overview** | Stats dashboard — duration, iterations, token totals, tool call counts, per-iteration token bar chart, tool usage table |\\n| **Timeline** | Chronological event stream — model calls, tool executions, warnings, and errors merged into a single timeline |\\n| **Messages** | Full message history — user, assistant, system, and tool messages with collapsible thinking blocks |\\n| **Logs** | Structured log entries — timestamp, level, message, and metadata fields |\\n| **Traces** | Hierarchical span tree — OpenTelemetry-style view with duration, status, and attributes per span |\\n\\n### Global views\\n\\n| Tab | What it shows |\\n|-----|---------------|\\n| **Config** | Resolved configuration (API keys redacted) |\\n| **Context** | Discovered context files and glob patterns |\\n| **Middleware** | Active middleware hooks and registered functions |\\n| **Memory** | Browse, search, add, and delete persistent memories |\\n\\n## Overview dashboard\\n\\n![Overview dashboard](/inspector-overview.png)\\n\\nThe Overview tab aggregates trace data into an at-a-glance summary:\\n\\n- **Stats cards** — total duration, iteration count, input/output/thinking tokens, tool calls, tool errors, message count, and loop status (ok/error)\\n- **Token chart** — horizontal bar per iteration showing input (blue), output (green), and thinking (purple) token usage relative to the most expensive iteration\\n- **Tool table** — every tool used in the session, sorted by call count, with error count and total/average execution time\\n\\n> The Overview tab requires observability traces. Traces are enabled by default — if you see \\"No trace data available\\", check that tracing hasn\'t been disabled:\\n>\\n> ```yaml\\n> tracesEnabled: true   # default: true\\n> logsEnabled: true     # default: true\\n> ```\\n\\n## Timeline\\n\\n![Timeline view](/inspector-timeline.png)\\n\\nThe Timeline tab merges two data sources into one chronological view:\\n\\n1. **Trace spans** — `agent.loop`, `agent.iteration`, `agent.model_call`, `agent.tool_execution`\\n2. **Log entries** — only `warn` and `error` level entries (to keep the timeline focused)\\n\\nEach event shows its timestamp, duration, and type. Events are color-coded:\\n\\n- Blue — model calls\\n- Orange — tool executions\\n- Green — iterations\\n- Purple — loop start/end\\n- Red — errors\\n\\n## Configuration\\n\\n```yaml\\napp:\\n  inspector:\\n    port: 3002\\n```\\n\\n| Field | Env var | CLI flag | Default | Description |\\n|-------|---------|----------|---------|-------------|\\n| `inspector.port` | `RA_INSPECTOR_PORT` | `--inspector-port` | `3002` | Port for the inspector server |\\n\\nThe inspector is enabled with the `--inspector` flag. It launches as its own interface — it cannot run as a sidecar alongside another interface (CLI, REPL, HTTP, MCP). Instead, run your agent sessions first, then launch the inspector separately to review them.\\n\\n## API endpoints\\n\\nThe inspector serves a JSON API that the dashboard consumes. You can also query it directly:\\n\\n| Endpoint | Description |\\n|----------|-------------|\\n| `GET /api/sessions` | List all sessions (sorted newest first) |\\n| `GET /api/sessions/:id/stats` | Aggregated stats from traces |\\n| `GET /api/sessions/:id/timeline` | Chronological event stream |\\n| `GET /api/sessions/:id/messages` | Raw message history |\\n| `GET /api/sessions/:id/logs` | Structured log entries |\\n| `GET /api/sessions/:id/traces` | Raw trace spans |\\n| `GET /api/config` | Resolved config (keys redacted) |\\n| `GET /api/context` | Discovered context files |\\n| `GET /api/middleware` | Active middleware hooks |\\n| `GET /api/memory` | List or search memories |\\n| `POST /api/memory` | Create a memory |\\n| `DELETE /api/memory/:id` | Delete a memory |\\n\\n## Example\\n\\n```bash\\n# Run an agent session first\\nra \\"Explain the architecture of this project\\"\\n\\n# Then launch the inspector to review it\\nra --inspector\\n\\n# In another terminal, query the API directly\\ncurl http://localhost:3002/api/sessions | jq \'.[0].id\'\\ncurl http://localhost:3002/api/sessions/SESSION_ID/stats | jq \'.totalTokens\'\\n```\\n\\n## See also\\n\\n- [Observability](/observability/) — traces and logs that feed the inspector\\n- [Sessions](/core/sessions) — session persistence and storage\\n- [Configuration](/configuration/) — full config reference\\n"}'),r={name:"modes/inspector.md"};function l(d,e,h,c,p,g){return a(),i("div",null,[...e[0]||(e[0]=[n(`<h1 id="inspector" tabindex="-1">Inspector <a class="header-anchor" href="#inspector" aria-label="Permalink to &quot;Inspector&quot;">​</a></h1><p>A standalone web dashboard for debugging agent sessions. Serves a single-page app that lets you browse sessions, inspect messages, view token usage, and trace every tool call the agent made.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ra</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --inspector</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                          # launch on default port 3002</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ra</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --inspector</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --inspector-port</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 8080</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # custom port</span></span></code></pre></div><blockquote><p>The inspector is a standalone interface — it reads saved session data from disk. Run your agent normally (CLI, REPL, HTTP, etc.), then launch the inspector separately to review sessions.</p></blockquote><p>Open <code>http://localhost:3002</code> in your browser.</p><h2 id="views" tabindex="-1">Views <a class="header-anchor" href="#views" aria-label="Permalink to &quot;Views&quot;">​</a></h2><p><img src="`+t+'" alt="Inspector Overview"></p><h3 id="session-views" tabindex="-1">Session views <a class="header-anchor" href="#session-views" aria-label="Permalink to &quot;Session views&quot;">​</a></h3><p>Select a session from the sidebar to see its data across five tabs:</p><table tabindex="0"><thead><tr><th>Tab</th><th>What it shows</th></tr></thead><tbody><tr><td><strong>Overview</strong></td><td>Stats dashboard — duration, iterations, token totals, tool call counts, per-iteration token bar chart, tool usage table</td></tr><tr><td><strong>Timeline</strong></td><td>Chronological event stream — model calls, tool executions, warnings, and errors merged into a single timeline</td></tr><tr><td><strong>Messages</strong></td><td>Full message history — user, assistant, system, and tool messages with collapsible thinking blocks</td></tr><tr><td><strong>Logs</strong></td><td>Structured log entries — timestamp, level, message, and metadata fields</td></tr><tr><td><strong>Traces</strong></td><td>Hierarchical span tree — OpenTelemetry-style view with duration, status, and attributes per span</td></tr></tbody></table><h3 id="global-views" tabindex="-1">Global views <a class="header-anchor" href="#global-views" aria-label="Permalink to &quot;Global views&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Tab</th><th>What it shows</th></tr></thead><tbody><tr><td><strong>Config</strong></td><td>Resolved configuration (API keys redacted)</td></tr><tr><td><strong>Context</strong></td><td>Discovered context files and glob patterns</td></tr><tr><td><strong>Middleware</strong></td><td>Active middleware hooks and registered functions</td></tr><tr><td><strong>Memory</strong></td><td>Browse, search, add, and delete persistent memories</td></tr></tbody></table><h2 id="overview-dashboard" tabindex="-1">Overview dashboard <a class="header-anchor" href="#overview-dashboard" aria-label="Permalink to &quot;Overview dashboard&quot;">​</a></h2><p><img src="'+t+`" alt="Overview dashboard"></p><p>The Overview tab aggregates trace data into an at-a-glance summary:</p><ul><li><strong>Stats cards</strong> — total duration, iteration count, input/output/thinking tokens, tool calls, tool errors, message count, and loop status (ok/error)</li><li><strong>Token chart</strong> — horizontal bar per iteration showing input (blue), output (green), and thinking (purple) token usage relative to the most expensive iteration</li><li><strong>Tool table</strong> — every tool used in the session, sorted by call count, with error count and total/average execution time</li></ul><blockquote><p>The Overview tab requires observability traces. Traces are enabled by default — if you see &quot;No trace data available&quot;, check that tracing hasn&#39;t been disabled:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">tracesEnabled</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # default: true</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">logsEnabled</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     # default: true</span></span></code></pre></div></blockquote><h2 id="timeline" tabindex="-1">Timeline <a class="header-anchor" href="#timeline" aria-label="Permalink to &quot;Timeline&quot;">​</a></h2><p><img src="`+o+`" alt="Timeline view"></p><p>The Timeline tab merges two data sources into one chronological view:</p><ol><li><strong>Trace spans</strong> — <code>agent.loop</code>, <code>agent.iteration</code>, <code>agent.model_call</code>, <code>agent.tool_execution</code></li><li><strong>Log entries</strong> — only <code>warn</code> and <code>error</code> level entries (to keep the timeline focused)</li></ol><p>Each event shows its timestamp, duration, and type. Events are color-coded:</p><ul><li>Blue — model calls</li><li>Orange — tool executions</li><li>Green — iterations</li><li>Purple — loop start/end</li><li>Red — errors</li></ul><h2 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to &quot;Configuration&quot;">​</a></h2><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">app</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  inspector</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    port</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3002</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Field</th><th>Env var</th><th>CLI flag</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>inspector.port</code></td><td><code>RA_INSPECTOR_PORT</code></td><td><code>--inspector-port</code></td><td><code>3002</code></td><td>Port for the inspector server</td></tr></tbody></table><p>The inspector is enabled with the <code>--inspector</code> flag. It launches as its own interface — it cannot run as a sidecar alongside another interface (CLI, REPL, HTTP, MCP). Instead, run your agent sessions first, then launch the inspector separately to review them.</p><h2 id="api-endpoints" tabindex="-1">API endpoints <a class="header-anchor" href="#api-endpoints" aria-label="Permalink to &quot;API endpoints&quot;">​</a></h2><p>The inspector serves a JSON API that the dashboard consumes. You can also query it directly:</p><table tabindex="0"><thead><tr><th>Endpoint</th><th>Description</th></tr></thead><tbody><tr><td><code>GET /api/sessions</code></td><td>List all sessions (sorted newest first)</td></tr><tr><td><code>GET /api/sessions/:id/stats</code></td><td>Aggregated stats from traces</td></tr><tr><td><code>GET /api/sessions/:id/timeline</code></td><td>Chronological event stream</td></tr><tr><td><code>GET /api/sessions/:id/messages</code></td><td>Raw message history</td></tr><tr><td><code>GET /api/sessions/:id/logs</code></td><td>Structured log entries</td></tr><tr><td><code>GET /api/sessions/:id/traces</code></td><td>Raw trace spans</td></tr><tr><td><code>GET /api/config</code></td><td>Resolved config (keys redacted)</td></tr><tr><td><code>GET /api/context</code></td><td>Discovered context files</td></tr><tr><td><code>GET /api/middleware</code></td><td>Active middleware hooks</td></tr><tr><td><code>GET /api/memory</code></td><td>List or search memories</td></tr><tr><td><code>POST /api/memory</code></td><td>Create a memory</td></tr><tr><td><code>DELETE /api/memory/:id</code></td><td>Delete a memory</td></tr></tbody></table><h2 id="example" tabindex="-1">Example <a class="header-anchor" href="#example" aria-label="Permalink to &quot;Example&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Run an agent session first</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ra</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Explain the architecture of this project&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Then launch the inspector to review it</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ra</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --inspector</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># In another terminal, query the API directly</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> http://localhost:3002/api/sessions</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> jq</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;.[0].id&#39;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> http://localhost:3002/api/sessions/SESSION_ID/stats</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> jq</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;.totalTokens&#39;</span></span></code></pre></div><h2 id="see-also" tabindex="-1">See also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;See also&quot;">​</a></h2><ul><li><a href="/ra/dev/observability/">Observability</a> — traces and logs that feed the inspector</li><li><a href="/ra/dev/core/sessions.html">Sessions</a> — session persistence and storage</li><li><a href="/ra/dev/configuration/">Configuration</a> — full config reference</li></ul>`,34)])])}const m=s(r,[["render",l]]);export{k as __pageData,m as default};
