import{_ as s,o as a,c as n,ai as o}from"./chunks/framework.Cz-NuZmh.js";const k=JSON.parse('{"title":"The Agent Loop","description":"","frontmatter":{},"headers":[],"relativePath":"core/agent-loop.md","filePath":"core/agent-loop.md","rawMarkdown":"# The Agent Loop\\n\\nra\'s core loop is simple: send messages to the model, stream the response, execute any tool calls, repeat. Every step fires a [middleware hook](/middleware/) you can intercept. The loop handles iteration, token tracking, and tool execution — you control everything else through system prompts, skills, and middleware.\\n\\n```\\n┌─────────────────────────────────────────────────┐\\n│                  beforeLoopBegin                │\\n└──────────────────────┬──────────────────────────┘\\n                       ▼\\n         ┌─── beforeModelCall ◄────────────┐\\n         │                                 │\\n         ▼                                 │\\n    Stream response                        │\\n    (onStreamChunk)                        │\\n         │                                 │\\n         ▼                                 │\\n   afterModelResponse                      │\\n         │                                 │\\n         ├── No tool calls? ──► afterLoopComplete\\n         │\\n         ▼\\n   beforeToolExecution\\n         │\\n         ▼\\n    Execute tools\\n         │\\n         ▼\\n   afterToolExecution\\n         │\\n         ▼\\n   afterLoopIteration ────────────────────►┘\\n```\\n\\n## How it works\\n\\n1. **Start** — `beforeLoopBegin` fires once. Your middleware can set up logging, validate config, or inject initial context.\\n\\n2. **Model call** — `beforeModelCall` fires with the full request (messages, tools, model). The model streams its response, firing `onStreamChunk` for every token. When the response is complete, `afterModelResponse` fires.\\n\\n3. **Tool execution** — If the model requested tool calls, `beforeToolExecution` fires for each one, then the tool runs, then `afterToolExecution` fires with the result.\\n\\n4. **Iterate or complete** — If tools were called, `afterLoopIteration` fires and the loop goes back to step 2 with the tool results appended to the conversation. If no tools were called, `afterLoopComplete` fires and the loop ends.\\n\\n## Loop controls\\n\\nThe loop tracks token usage per iteration, enforces `maxIterations`, and supports an `AbortController`. Any middleware can call `ctx.stop()` to halt the loop cleanly.\\n\\n```yaml\\nagent:\\n  maxIterations: 50   # default — prevents runaway loops\\n  toolTimeout: 30000  # per-tool timeout in ms\\n```\\n\\n```ts\\n// middleware/budget.ts — stop if we\'ve used too many tokens\\nexport default async (ctx) => {\\n  if (ctx.loop.usage.inputTokens + ctx.loop.usage.outputTokens > 100_000) {\\n    ctx.stop()\\n  }\\n}\\n```\\n\\n## See also\\n\\n- [Middleware](/middleware/) — all available hooks and their context shapes\\n- [Built-in Tools](/tools/) — tools available to the agent\\n- [Context Control](/core/context-control) — how ra manages what the model sees\\n- [Configuration](/configuration/) — `maxIterations` and `toolTimeout` settings\\n"}'),t={name:"core/agent-loop.md"};function l(i,e,p,r,h,d){return a(),n("div",null,[...e[0]||(e[0]=[o(`<h1 id="the-agent-loop" tabindex="-1">The Agent Loop <a class="header-anchor" href="#the-agent-loop" aria-label="Permalink to &quot;The Agent Loop&quot;">​</a></h1><p>ra&#39;s core loop is simple: send messages to the model, stream the response, execute any tool calls, repeat. Every step fires a <a href="/ra/dev/middleware/">middleware hook</a> you can intercept. The loop handles iteration, token tracking, and tool execution — you control everything else through system prompts, skills, and middleware.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>┌─────────────────────────────────────────────────┐</span></span>
<span class="line"><span>│                  beforeLoopBegin                │</span></span>
<span class="line"><span>└──────────────────────┬──────────────────────────┘</span></span>
<span class="line"><span>                       ▼</span></span>
<span class="line"><span>         ┌─── beforeModelCall ◄────────────┐</span></span>
<span class="line"><span>         │                                 │</span></span>
<span class="line"><span>         ▼                                 │</span></span>
<span class="line"><span>    Stream response                        │</span></span>
<span class="line"><span>    (onStreamChunk)                        │</span></span>
<span class="line"><span>         │                                 │</span></span>
<span class="line"><span>         ▼                                 │</span></span>
<span class="line"><span>   afterModelResponse                      │</span></span>
<span class="line"><span>         │                                 │</span></span>
<span class="line"><span>         ├── No tool calls? ──► afterLoopComplete</span></span>
<span class="line"><span>         │</span></span>
<span class="line"><span>         ▼</span></span>
<span class="line"><span>   beforeToolExecution</span></span>
<span class="line"><span>         │</span></span>
<span class="line"><span>         ▼</span></span>
<span class="line"><span>    Execute tools</span></span>
<span class="line"><span>         │</span></span>
<span class="line"><span>         ▼</span></span>
<span class="line"><span>   afterToolExecution</span></span>
<span class="line"><span>         │</span></span>
<span class="line"><span>         ▼</span></span>
<span class="line"><span>   afterLoopIteration ────────────────────►┘</span></span></code></pre></div><h2 id="how-it-works" tabindex="-1">How it works <a class="header-anchor" href="#how-it-works" aria-label="Permalink to &quot;How it works&quot;">​</a></h2><ol><li><p><strong>Start</strong> — <code>beforeLoopBegin</code> fires once. Your middleware can set up logging, validate config, or inject initial context.</p></li><li><p><strong>Model call</strong> — <code>beforeModelCall</code> fires with the full request (messages, tools, model). The model streams its response, firing <code>onStreamChunk</code> for every token. When the response is complete, <code>afterModelResponse</code> fires.</p></li><li><p><strong>Tool execution</strong> — If the model requested tool calls, <code>beforeToolExecution</code> fires for each one, then the tool runs, then <code>afterToolExecution</code> fires with the result.</p></li><li><p><strong>Iterate or complete</strong> — If tools were called, <code>afterLoopIteration</code> fires and the loop goes back to step 2 with the tool results appended to the conversation. If no tools were called, <code>afterLoopComplete</code> fires and the loop ends.</p></li></ol><h2 id="loop-controls" tabindex="-1">Loop controls <a class="header-anchor" href="#loop-controls" aria-label="Permalink to &quot;Loop controls&quot;">​</a></h2><p>The loop tracks token usage per iteration, enforces <code>maxIterations</code>, and supports an <code>AbortController</code>. Any middleware can call <code>ctx.stop()</code> to halt the loop cleanly.</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">agent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  maxIterations</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">50</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # default — prevents runaway loops</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  toolTimeout</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">30000</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # per-tool timeout in ms</span></span></code></pre></div><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// middleware/budget.ts — stop if we&#39;ve used too many tokens</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (ctx.loop.usage.inputTokens </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.loop.usage.outputTokens </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 100_000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ctx.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">stop</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h2 id="see-also" tabindex="-1">See also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;See also&quot;">​</a></h2><ul><li><a href="/ra/dev/middleware/">Middleware</a> — all available hooks and their context shapes</li><li><a href="/ra/dev/tools/">Built-in Tools</a> — tools available to the agent</li><li><a href="/ra/dev/core/context-control.html">Context Control</a> — how ra manages what the model sees</li><li><a href="/ra/dev/configuration/">Configuration</a> — <code>maxIterations</code> and <code>toolTimeout</code> settings</li></ul>`,11)])])}const g=s(t,[["render",l]]);export{k as __pageData,g as default};
