import{_ as s,o as t,c as a,ai as i}from"./chunks/framework.Cz-NuZmh.js";const g=JSON.parse('{"title":"Hot-Reload","description":"","frontmatter":{},"headers":[],"relativePath":"core/hot-reload.md","filePath":"core/hot-reload.md","rawMarkdown":"# Hot-Reload\\n\\nra watches your config file and all files it references — system prompts, middleware scripts, custom tools, context resolvers — and reloads them automatically between agent loops. Edit a file, send the next message, and the agent picks up your changes. No restart needed.\\n\\n## How it works\\n\\nBefore each agent loop (each REPL input or HTTP request), ra checks the mtime of every tracked file. If any file has been modified since the last check, ra:\\n\\n1. **Reloads the config** — re-reads and re-merges `ra.config.yaml` with defaults, env vars, and CLI args\\n2. **Rebuilds the provider** — if the provider name or credentials changed\\n3. **Rebuilds tools** — re-imports custom tool files, re-registers builtins\\n4. **Rebuilds middleware** — re-imports middleware scripts, re-wires system hooks\\n5. **Rebuilds skills** — re-scans skill directories\\n6. **Rebuilds context** — re-discovers context files if patterns changed\\n\\nLong-lived state is preserved: storage, MCP server connections, memory and scratchpad stores, and observability pipelines are never disrupted.\\n\\n## What\'s tracked\\n\\n| File type | Example | Detected how |\\n|-----------|---------|-------------|\\n| Config file | `ra.config.yaml` | Always tracked |\\n| System prompt | `./prompt.txt` | Tracked when `systemPrompt` points to a file |\\n| Middleware scripts | `./middleware/budget.ts` | File path entries in `agent.middleware` |\\n| Custom tools | `./tools/search.ts` | Entries in `agent.tools.custom` |\\n| Context resolvers | `./resolvers/jira.ts` | `agent.context.resolvers[].path` |\\n\\nInline middleware expressions (`(ctx) => { ... }`) and `shell:` entries are not tracked — only file paths.\\n\\n## Configuration\\n\\nHot-reload is **enabled by default**. To disable it:\\n\\n```yaml\\nagent:\\n  hotReload: false\\n```\\n\\nWhen disabled, config is loaded once at startup and never re-checked. This saves a few `stat()` calls per request if your config never changes at runtime.\\n\\n## Example: live system prompt editing\\n\\n```yaml\\n# ra.config.yaml\\nagent:\\n  systemPrompt: ./prompts/system.md\\n```\\n\\n```bash\\n# Terminal 1: start the agent\\nra\\n\\n# Terminal 2: edit the prompt while the agent is running\\necho \\"You are a pirate. Respond only in pirate speak.\\" > prompts/system.md\\n```\\n\\nThe next message you send in Terminal 1 uses the new prompt — no restart.\\n\\n## Example: iterating on a custom tool\\n\\n```yaml\\nagent:\\n  tools:\\n    custom:\\n      - ./tools/deploy.ts\\n```\\n\\nEdit `tools/deploy.ts` in your editor. The next time the agent calls the tool, it runs the updated code.\\n\\n## Example: tweaking middleware\\n\\n```yaml\\nagent:\\n  middleware:\\n    afterToolExecution:\\n      - ./middleware/audit.ts\\n```\\n\\nModify `audit.ts` to change what gets logged. Changes take effect on the next agent loop.\\n\\n## Concurrency\\n\\nFor HTTP servers handling concurrent requests, reloads are serialized — if two requests arrive simultaneously and both detect a file change, only one reload runs. The second request waits for the first reload to finish and reuses its result.\\n\\n## Caveats\\n\\n- **MCP connections are not restarted.** Adding or removing `app.mcpServers` entries requires a restart. Existing MCP tools are preserved across reloads.\\n- **App-level settings** (`app.interface`, `app.http.port`, `app.dataDir`) are bound at startup. Changing them in the config has no effect until restart.\\n- **Module cache is cleared on reload.** When a `.ts` or `.js` file is re-imported, the previous module is evicted from the cache. This means module-level state (counters, caches) resets on each reload.\\n"}'),n={name:"core/hot-reload.md"};function r(o,e,l,d,p,h){return t(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="hot-reload" tabindex="-1">Hot-Reload <a class="header-anchor" href="#hot-reload" aria-label="Permalink to &quot;Hot-Reload&quot;">​</a></h1><p>ra watches your config file and all files it references — system prompts, middleware scripts, custom tools, context resolvers — and reloads them automatically between agent loops. Edit a file, send the next message, and the agent picks up your changes. No restart needed.</p><h2 id="how-it-works" tabindex="-1">How it works <a class="header-anchor" href="#how-it-works" aria-label="Permalink to &quot;How it works&quot;">​</a></h2><p>Before each agent loop (each REPL input or HTTP request), ra checks the mtime of every tracked file. If any file has been modified since the last check, ra:</p><ol><li><strong>Reloads the config</strong> — re-reads and re-merges <code>ra.config.yaml</code> with defaults, env vars, and CLI args</li><li><strong>Rebuilds the provider</strong> — if the provider name or credentials changed</li><li><strong>Rebuilds tools</strong> — re-imports custom tool files, re-registers builtins</li><li><strong>Rebuilds middleware</strong> — re-imports middleware scripts, re-wires system hooks</li><li><strong>Rebuilds skills</strong> — re-scans skill directories</li><li><strong>Rebuilds context</strong> — re-discovers context files if patterns changed</li></ol><p>Long-lived state is preserved: storage, MCP server connections, memory and scratchpad stores, and observability pipelines are never disrupted.</p><h2 id="what-s-tracked" tabindex="-1">What&#39;s tracked <a class="header-anchor" href="#what-s-tracked" aria-label="Permalink to &quot;What&#39;s tracked&quot;">​</a></h2><table tabindex="0"><thead><tr><th>File type</th><th>Example</th><th>Detected how</th></tr></thead><tbody><tr><td>Config file</td><td><code>ra.config.yaml</code></td><td>Always tracked</td></tr><tr><td>System prompt</td><td><code>./prompt.txt</code></td><td>Tracked when <code>systemPrompt</code> points to a file</td></tr><tr><td>Middleware scripts</td><td><code>./middleware/budget.ts</code></td><td>File path entries in <code>agent.middleware</code></td></tr><tr><td>Custom tools</td><td><code>./tools/search.ts</code></td><td>Entries in <code>agent.tools.custom</code></td></tr><tr><td>Context resolvers</td><td><code>./resolvers/jira.ts</code></td><td><code>agent.context.resolvers[].path</code></td></tr></tbody></table><p>Inline middleware expressions (<code>(ctx) =&gt; { ... }</code>) and <code>shell:</code> entries are not tracked — only file paths.</p><h2 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to &quot;Configuration&quot;">​</a></h2><p>Hot-reload is <strong>enabled by default</strong>. To disable it:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">agent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  hotReload</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span></code></pre></div><p>When disabled, config is loaded once at startup and never re-checked. This saves a few <code>stat()</code> calls per request if your config never changes at runtime.</p><h2 id="example-live-system-prompt-editing" tabindex="-1">Example: live system prompt editing <a class="header-anchor" href="#example-live-system-prompt-editing" aria-label="Permalink to &quot;Example: live system prompt editing&quot;">​</a></h2><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ra.config.yaml</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">agent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  systemPrompt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">./prompts/system.md</span></span></code></pre></div><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Terminal 1: start the agent</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ra</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Terminal 2: edit the prompt while the agent is running</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;You are a pirate. Respond only in pirate speak.&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> prompts/system.md</span></span></code></pre></div><p>The next message you send in Terminal 1 uses the new prompt — no restart.</p><h2 id="example-iterating-on-a-custom-tool" tabindex="-1">Example: iterating on a custom tool <a class="header-anchor" href="#example-iterating-on-a-custom-tool" aria-label="Permalink to &quot;Example: iterating on a custom tool&quot;">​</a></h2><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">agent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  tools</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    custom</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">./tools/deploy.ts</span></span></code></pre></div><p>Edit <code>tools/deploy.ts</code> in your editor. The next time the agent calls the tool, it runs the updated code.</p><h2 id="example-tweaking-middleware" tabindex="-1">Example: tweaking middleware <a class="header-anchor" href="#example-tweaking-middleware" aria-label="Permalink to &quot;Example: tweaking middleware&quot;">​</a></h2><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">agent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  middleware</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    afterToolExecution</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">./middleware/audit.ts</span></span></code></pre></div><p>Modify <code>audit.ts</code> to change what gets logged. Changes take effect on the next agent loop.</p><h2 id="concurrency" tabindex="-1">Concurrency <a class="header-anchor" href="#concurrency" aria-label="Permalink to &quot;Concurrency&quot;">​</a></h2><p>For HTTP servers handling concurrent requests, reloads are serialized — if two requests arrive simultaneously and both detect a file change, only one reload runs. The second request waits for the first reload to finish and reuses its result.</p><h2 id="caveats" tabindex="-1">Caveats <a class="header-anchor" href="#caveats" aria-label="Permalink to &quot;Caveats&quot;">​</a></h2><ul><li><strong>MCP connections are not restarted.</strong> Adding or removing <code>app.mcpServers</code> entries requires a restart. Existing MCP tools are preserved across reloads.</li><li><strong>App-level settings</strong> (<code>app.interface</code>, <code>app.http.port</code>, <code>app.dataDir</code>) are bound at startup. Changing them in the config has no effect until restart.</li><li><strong>Module cache is cleared on reload.</strong> When a <code>.ts</code> or <code>.js</code> file is re-imported, the previous module is evicted from the cache. This means module-level state (counters, caches) resets on each reload.</li></ul>`,27)])])}const m=s(n,[["render",r]]);export{g as __pageData,m as default};
