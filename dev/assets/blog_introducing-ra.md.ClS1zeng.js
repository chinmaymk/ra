import{_ as s,o as i,c as a,ai as n}from"./chunks/framework.Cz-NuZmh.js";const g=JSON.parse('{"title":"Introducing ra","description":"","frontmatter":{},"headers":[],"relativePath":"blog/introducing-ra.md","filePath":"blog/introducing-ra.md","rawMarkdown":"# Introducing ra\\n\\n<span class=\\"blog-date\\">March 30, 2026</span>\\n\\nra is an open-source agent harness. Give it a task, point it at an LLM, and let it work — reading files, running commands, calling APIs, and looping until the job is done. You stay in control through a config file, middleware hooks, and permission rules. Nothing is hidden behind abstractions you can\'t reach.\\n\\nThis post walks through the ideas behind ra and the features that make it useful.\\n\\n## The agent loop is the product\\n\\nAt the core of ra is a simple loop: call the model, execute the tool calls it returns, feed the results back, repeat. That\'s it. No planning graphs, no state machines, no orchestration DSL. The loop runs until the model says it\'s done — no arbitrary iteration caps.\\n\\n```\\nUser message → model → tool calls → execute → model → … → done\\n```\\n\\nWhat makes it powerful is what you can hook into. Nine middleware hooks span the entire lifecycle — `beforeModelCall`, `afterModelResponse`, `beforeToolExecution`, `afterToolExecution`, and more. Every hook can inspect, modify, or block the step it wraps. Want to enforce a token budget? Write a `beforeModelCall` hook. Want to redact sensitive output? Write an `afterToolExecution` hook. Want to halt the loop after a certain duration? `afterLoopIteration`.\\n\\n```typescript\\n// middleware/budget.ts — stop the agent when tokens run out\\nexport default async function ({ context }) {\\n  if (context.totalTokens > 500_000) {\\n    return { stop: true, reason: \'Token budget exceeded\' }\\n  }\\n}\\n```\\n\\nMiddleware is just TypeScript files. No plugin API to learn, no registration boilerplate.\\n\\n## Context engineering as a first-class concern\\n\\nMost agent frameworks treat the context window as a bucket you throw messages into until it overflows. ra treats it as a resource to be managed.\\n\\n**Smart compaction.** When the conversation grows too large, ra compacts it — either by truncating older messages (preserving cached prefixes for cost savings) or by summarizing them into a condensed history. The compaction strategy is configurable and respects provider-specific prompt caching (Anthropic, OpenAI, Google) so you don\'t blow away cache hits unnecessarily.\\n\\n**Dynamic context window learning.** If a provider rejects a request because it exceeded the real context limit, ra learns the actual size and adapts future compaction thresholds automatically.\\n\\n**Automatic discovery.** Drop a `CLAUDE.md`, `AGENTS.md`, or `.cursorrules` file in your project and ra picks it up — convention files are injected into the system prompt automatically.\\n\\n**Pattern resolution.** Reference files inline in your prompts with `@src/auth.ts` or `@src/**/*.ts`, and ra expands them into the context. Same for URLs: `url:https://example.com/docs` fetches and inlines the content.\\n\\n## See what the agent is doing\\n\\nEvery ra session produces structured logs, trace spans, and per-iteration token metrics — automatically, with no instrumentation code.\\n\\nThe built-in **Inspector** is a web dashboard that shows you every model call, every tool execution, every thinking block, and exactly how tokens were spent across the session. When an agent does something unexpected, you don\'t guess — you look.\\n\\nTraces form a hierarchy: `agent.loop` → `agent.iteration` → `agent.model_call` / `agent.tool_execution`, each with duration, status, and attributes. Pipe the JSONL logs to your own tooling or just read them directly.\\n\\n## The config is the agent\\n\\nA single `ra.config.yml` turns any directory into a purpose-built agent. Define the provider, model, tools, permissions, middleware, and skills — all in one file.\\n\\n```yaml\\nagent:\\n  provider: anthropic\\n  model: claude-sonnet-4-6\\n  thinking: adaptive\\n  maxTokenBudget: 500_000\\n\\n  permissions:\\n    rules:\\n      - tool: Bash\\n        command:\\n          allow: [\\"^git \\", \\"^bun \\"]\\n          deny: [\\"--force\\", \\"--hard\\"]\\n\\n  middleware:\\n    beforeModelCall:\\n      - \\"./middleware/budget.ts\\"\\n\\n  skillDirs:\\n    - ./skills\\n```\\n\\nPermissions are regex-based allow/deny rules per tool, per field. The config above lets the agent run `git` and `bun` commands but blocks `--force` and `--hard` flags. No code required — just patterns.\\n\\n**Adaptive thinking** deserves a mention. ra supports extended thinking where the model reasons deeply before responding. In adaptive mode, the agent uses high thinking budget for the first iterations (when the problem is being understood) and dials it back as execution progresses. You get deep reasoning when it matters without burning tokens on routine follow-up.\\n\\n## Skills: composable agent roles\\n\\nSkills are reusable instruction bundles — a directory with a `SKILL.md` file and optional scripts or reference files. Think of them as roles you can assign to the agent.\\n\\n```bash\\nra --skill code-review \\"Review these changes\\"\\nra --skill debugger --file error.log \\"Why is this failing?\\"\\n```\\n\\nSkills use progressive disclosure. The model initially sees only skill names and one-line descriptions. When it decides a skill is relevant, the full instructions are loaded. This keeps the context lean until depth is needed.\\n\\nYou can install skills from GitHub, npm, or local directories. And here\'s the interesting part: **the agent can write new skills at runtime** — extending its own capabilities as it works.\\n\\n## Recipes: shareable agent configurations\\n\\nA recipe is a complete agent setup — config, skills, and middleware — packaged as a directory. Run a recipe and you get a fully configured agent:\\n\\n```bash\\nra --recipe coding-agent \\"Fix the failing tests and open a PR\\"\\nra --recipe code-review-agent \\"Review this diff\\"\\n```\\n\\nRecipes layer on top of your existing config. Skills and middleware from a recipe prepend to yours rather than replacing them. This means you can start from a recipe and customize from there.\\n\\n## Runs anywhere\\n\\nra is a single binary. The same agent runs across multiple interfaces:\\n\\n- **CLI** — one-shot prompts, piping, chaining. `cat error.log | ra \\"Explain this error\\"` just works.\\n- **REPL** — interactive sessions with history, slash commands, file attachments.\\n- **HTTP API** — sync and streaming endpoints for building on top of ra.\\n- **MCP server** — `ra --mcp-stdio` exposes the agent to Cursor, Claude Desktop, or any MCP-compatible editor.\\n- **Cron** — scheduled autonomous jobs with isolated sessions and logs.\\n- **GitHub Actions** — run ra in CI/CD with no install step.\\n\\nAnd it works with every major provider: Anthropic, OpenAI, Google, Ollama, AWS Bedrock, and Azure. Switch with a flag.\\n\\n## Sessions and memory\\n\\nConversations persist as JSONL files, scoped per-project. Start a session in the REPL, resume it later from the HTTP API. Sessions are the same format everywhere.\\n\\nFor longer-lived knowledge, ra has a built-in **memory** system backed by SQLite with full-text search. Agents can save facts, search them across sessions, and forget them when they\'re no longer relevant. This is how an agent remembers project conventions, past decisions, or user preferences across runs.\\n\\n## Get started\\n\\n```bash\\ncurl -fsSL https://raw.githubusercontent.com/chinmaymk/ra/main/install.sh | bash\\nexport ANTHROPIC_API_KEY=\\"sk-...\\"\\nra \\"Summarize the key points of this file\\" --file report.pdf\\n```\\n\\nRead the [docs](/getting-started/quick-start) or browse the [source on GitHub](https://github.com/chinmaymk/ra).\\n\\n<style>\\n.blog-date {\\n  display: inline-block;\\n  font-size: 0.85rem;\\n  color: var(--vp-c-text-2);\\n  margin-bottom: 1rem;\\n}\\n</style>\\n"}'),t={name:"blog/introducing-ra.md"};function o(r,e,l,h,p,d){return i(),a("div",null,[...e[0]||(e[0]=[n(`<h1 id="introducing-ra" tabindex="-1">Introducing ra <a class="header-anchor" href="#introducing-ra" aria-label="Permalink to &quot;Introducing ra&quot;">​</a></h1><p><span class="blog-date">March 30, 2026</span></p><p>ra is an open-source agent harness. Give it a task, point it at an LLM, and let it work — reading files, running commands, calling APIs, and looping until the job is done. You stay in control through a config file, middleware hooks, and permission rules. Nothing is hidden behind abstractions you can&#39;t reach.</p><p>This post walks through the ideas behind ra and the features that make it useful.</p><h2 id="the-agent-loop-is-the-product" tabindex="-1">The agent loop is the product <a class="header-anchor" href="#the-agent-loop-is-the-product" aria-label="Permalink to &quot;The agent loop is the product&quot;">​</a></h2><p>At the core of ra is a simple loop: call the model, execute the tool calls it returns, feed the results back, repeat. That&#39;s it. No planning graphs, no state machines, no orchestration DSL. The loop runs until the model says it&#39;s done — no arbitrary iteration caps.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>User message → model → tool calls → execute → model → … → done</span></span></code></pre></div><p>What makes it powerful is what you can hook into. Nine middleware hooks span the entire lifecycle — <code>beforeModelCall</code>, <code>afterModelResponse</code>, <code>beforeToolExecution</code>, <code>afterToolExecution</code>, and more. Every hook can inspect, modify, or block the step it wraps. Want to enforce a token budget? Write a <code>beforeModelCall</code> hook. Want to redact sensitive output? Write an <code>afterToolExecution</code> hook. Want to halt the loop after a certain duration? <code>afterLoopIteration</code>.</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// middleware/budget.ts — stop the agent when tokens run out</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> async</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ({ </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">context</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (context.totalTokens </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 500_000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { stop: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, reason: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Token budget exceeded&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Middleware is just TypeScript files. No plugin API to learn, no registration boilerplate.</p><h2 id="context-engineering-as-a-first-class-concern" tabindex="-1">Context engineering as a first-class concern <a class="header-anchor" href="#context-engineering-as-a-first-class-concern" aria-label="Permalink to &quot;Context engineering as a first-class concern&quot;">​</a></h2><p>Most agent frameworks treat the context window as a bucket you throw messages into until it overflows. ra treats it as a resource to be managed.</p><p><strong>Smart compaction.</strong> When the conversation grows too large, ra compacts it — either by truncating older messages (preserving cached prefixes for cost savings) or by summarizing them into a condensed history. The compaction strategy is configurable and respects provider-specific prompt caching (Anthropic, OpenAI, Google) so you don&#39;t blow away cache hits unnecessarily.</p><p><strong>Dynamic context window learning.</strong> If a provider rejects a request because it exceeded the real context limit, ra learns the actual size and adapts future compaction thresholds automatically.</p><p><strong>Automatic discovery.</strong> Drop a <code>CLAUDE.md</code>, <code>AGENTS.md</code>, or <code>.cursorrules</code> file in your project and ra picks it up — convention files are injected into the system prompt automatically.</p><p><strong>Pattern resolution.</strong> Reference files inline in your prompts with <code>@src/auth.ts</code> or <code>@src/**/*.ts</code>, and ra expands them into the context. Same for URLs: <code>url:https://example.com/docs</code> fetches and inlines the content.</p><h2 id="see-what-the-agent-is-doing" tabindex="-1">See what the agent is doing <a class="header-anchor" href="#see-what-the-agent-is-doing" aria-label="Permalink to &quot;See what the agent is doing&quot;">​</a></h2><p>Every ra session produces structured logs, trace spans, and per-iteration token metrics — automatically, with no instrumentation code.</p><p>The built-in <strong>Inspector</strong> is a web dashboard that shows you every model call, every tool execution, every thinking block, and exactly how tokens were spent across the session. When an agent does something unexpected, you don&#39;t guess — you look.</p><p>Traces form a hierarchy: <code>agent.loop</code> → <code>agent.iteration</code> → <code>agent.model_call</code> / <code>agent.tool_execution</code>, each with duration, status, and attributes. Pipe the JSONL logs to your own tooling or just read them directly.</p><h2 id="the-config-is-the-agent" tabindex="-1">The config is the agent <a class="header-anchor" href="#the-config-is-the-agent" aria-label="Permalink to &quot;The config is the agent&quot;">​</a></h2><p>A single <code>ra.config.yml</code> turns any directory into a purpose-built agent. Define the provider, model, tools, permissions, middleware, and skills — all in one file.</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">agent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  provider</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">anthropic</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  model</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">claude-sonnet-4-6</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  thinking</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">adaptive</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  maxTokenBudget</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">500_000</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  permissions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    rules</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">tool</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">Bash</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        command</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">          allow</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;^git &quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;^bun &quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">          deny</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;--force&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;--hard&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  middleware</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    beforeModelCall</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;./middleware/budget.ts&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  skillDirs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">./skills</span></span></code></pre></div><p>Permissions are regex-based allow/deny rules per tool, per field. The config above lets the agent run <code>git</code> and <code>bun</code> commands but blocks <code>--force</code> and <code>--hard</code> flags. No code required — just patterns.</p><p><strong>Adaptive thinking</strong> deserves a mention. ra supports extended thinking where the model reasons deeply before responding. In adaptive mode, the agent uses high thinking budget for the first iterations (when the problem is being understood) and dials it back as execution progresses. You get deep reasoning when it matters without burning tokens on routine follow-up.</p><h2 id="skills-composable-agent-roles" tabindex="-1">Skills: composable agent roles <a class="header-anchor" href="#skills-composable-agent-roles" aria-label="Permalink to &quot;Skills: composable agent roles&quot;">​</a></h2><p>Skills are reusable instruction bundles — a directory with a <code>SKILL.md</code> file and optional scripts or reference files. Think of them as roles you can assign to the agent.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ra</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --skill</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> code-review</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Review these changes&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ra</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --skill</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> debugger</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --file</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> error.log</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Why is this failing?&quot;</span></span></code></pre></div><p>Skills use progressive disclosure. The model initially sees only skill names and one-line descriptions. When it decides a skill is relevant, the full instructions are loaded. This keeps the context lean until depth is needed.</p><p>You can install skills from GitHub, npm, or local directories. And here&#39;s the interesting part: <strong>the agent can write new skills at runtime</strong> — extending its own capabilities as it works.</p><h2 id="recipes-shareable-agent-configurations" tabindex="-1">Recipes: shareable agent configurations <a class="header-anchor" href="#recipes-shareable-agent-configurations" aria-label="Permalink to &quot;Recipes: shareable agent configurations&quot;">​</a></h2><p>A recipe is a complete agent setup — config, skills, and middleware — packaged as a directory. Run a recipe and you get a fully configured agent:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ra</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --recipe</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> coding-agent</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Fix the failing tests and open a PR&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ra</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --recipe</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> code-review-agent</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Review this diff&quot;</span></span></code></pre></div><p>Recipes layer on top of your existing config. Skills and middleware from a recipe prepend to yours rather than replacing them. This means you can start from a recipe and customize from there.</p><h2 id="runs-anywhere" tabindex="-1">Runs anywhere <a class="header-anchor" href="#runs-anywhere" aria-label="Permalink to &quot;Runs anywhere&quot;">​</a></h2><p>ra is a single binary. The same agent runs across multiple interfaces:</p><ul><li><strong>CLI</strong> — one-shot prompts, piping, chaining. <code>cat error.log | ra &quot;Explain this error&quot;</code> just works.</li><li><strong>REPL</strong> — interactive sessions with history, slash commands, file attachments.</li><li><strong>HTTP API</strong> — sync and streaming endpoints for building on top of ra.</li><li><strong>MCP server</strong> — <code>ra --mcp-stdio</code> exposes the agent to Cursor, Claude Desktop, or any MCP-compatible editor.</li><li><strong>Cron</strong> — scheduled autonomous jobs with isolated sessions and logs.</li><li><strong>GitHub Actions</strong> — run ra in CI/CD with no install step.</li></ul><p>And it works with every major provider: Anthropic, OpenAI, Google, Ollama, AWS Bedrock, and Azure. Switch with a flag.</p><h2 id="sessions-and-memory" tabindex="-1">Sessions and memory <a class="header-anchor" href="#sessions-and-memory" aria-label="Permalink to &quot;Sessions and memory&quot;">​</a></h2><p>Conversations persist as JSONL files, scoped per-project. Start a session in the REPL, resume it later from the HTTP API. Sessions are the same format everywhere.</p><p>For longer-lived knowledge, ra has a built-in <strong>memory</strong> system backed by SQLite with full-text search. Agents can save facts, search them across sessions, and forget them when they&#39;re no longer relevant. This is how an agent remembers project conventions, past decisions, or user preferences across runs.</p><h2 id="get-started" tabindex="-1">Get started <a class="header-anchor" href="#get-started" aria-label="Permalink to &quot;Get started&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -fsSL</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> https://raw.githubusercontent.com/chinmaymk/ra/main/install.sh</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> bash</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ANTHROPIC_API_KEY</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;sk-...&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ra</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Summarize the key points of this file&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --file</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> report.pdf</span></span></code></pre></div><p>Read the <a href="/ra/dev/getting-started/quick-start.html">docs</a> or browse the <a href="https://github.com/chinmaymk/ra" target="_blank" rel="noreferrer">source on GitHub</a>.</p>`,44)])])}const k=s(t,[["render",o]]);export{g as __pageData,k as default};
