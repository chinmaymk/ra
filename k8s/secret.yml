# Template — fill in base64-encoded values before applying.
# Generate with: echo -n 'your-value' | base64
apiVersion: v1
kind: Secret
metadata:
  name: ra-secrets
type: Opaque
data:
  anthropic-api-key: ""   # base64-encoded
  openai-api-key: ""      # base64-encoded
  http-token: ""           # base64-encoded
